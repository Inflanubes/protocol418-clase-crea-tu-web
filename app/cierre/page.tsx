import { SlideShell } from '@/components/deck/SlideShell';

export default function CierrePage() {
  return (
    <SlideShell>
      <h1 style={{ fontFamily: 'var(--font-display)', fontSize: '80px' }}>Cierre</h1>
      <p>CTA — pendiente Task 18.</p>
    </SlideShell>
  );
}
